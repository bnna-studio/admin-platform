# Base URL of the admin API (no trailing slash).
# Local development: http://localhost:3000
# Production:        https://api.moaicreative.com (or your Railway URL)
#
# Vite inlines this at build time, so a production build must be made
# with the production value already set.
VITE_API_URL=http://localhost:3000
